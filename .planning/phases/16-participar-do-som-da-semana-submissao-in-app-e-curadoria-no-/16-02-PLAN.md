---
phase: 16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - api/config.py
  - api/main.py
autonomous: true
requirements:
  - SUBMIT-02
  - SUBMIT-03
  - SUBMIT-09
  - SEC-SUBMIT-04

must_haves:
  truths:
    - "SubmissionRequest + SubmissionContact Pydantic models exist with per-field validation and the at-least-one-contact rule"
    - "The maximum-cardinality public submission payload (3 artistas + 1 produtor + 4 links, every string field at its exact char cap, all 3 contact fields at max) measures 3726 bytes — under the 4KB global body limit with 370 bytes of margin"
    - "Submissions persist in a Redis Hash + Sorted Set with a JSON fallback, keyed by id"
    - "The retention cap evicts only rejeitada/arquivada entries, never Pendente/Promovida"
  artifacts:
    - path: "api/config.py"
      provides: "submission_rate_limit_per_hour + submissions_cap settings"
      contains: "submission_rate_limit_per_hour"
    - path: "api/main.py"
      provides: "SubmissionContact, SubmissionRequest, storage helpers, cap eviction"
      contains: "class SubmissionRequest"
  key_links:
    - from: "SubmissionContact.model_validator"
      to: "422 validation response"
      via: "existing _validation_exception_handler"
      pattern: "model_validator"
    - from: "_save_submission"
      to: "submissions:data / submissions:index"
      via: "Redis hset + zadd with cap eviction"
      pattern: "_enforce_submissions_cap"
---

<objective>
Build the data layer for Phase 16: configurable settings, the SubmissionRequest/SubmissionContact
Pydantic models (introducing the codebase's first model_validator for the "at least one contact"
rule), and the Redis Hash + Sorted Set + JSON-fallback storage with status-aware cap eviction.

Purpose: Submissions are mutable, status-tracked, and capped — unlike the append-only
featured:history List. This plan establishes the correct storage primitive (Hash keyed by id +
Sorted Set for ordering) before any endpoint consumes it.
Output: models + storage functions turning the SUBMIT-02/03/09 + SEC-SUBMIT-04 stubs partially GREEN
at the unit level (endpoints come in Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-CONTEXT.md
@.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-RESEARCH.md

<interfaces>
<!-- Exact existing symbols to reuse. Do NOT re-implement these. -->
From api/config.py:
- `_safe_int(env_key, default)` — env-var integer parser; add new settings via `field(default_factory=...)`.
From api/main.py:
- `FeaturedArtist` (line 141) — {nome: str, url: str=""} with field_validators. Mirror the validator STYLE but define a DISTINCT nested model (SubmissionArtist) with TIGHTER caps (nome<=100, url<=200) — do NOT reuse FeaturedArtist directly (its admin 200/500 caps blow the 4KB public-body budget).
- `FeaturedLink` (line 169) — {label: str, url: str} with http validators. Mirror the STYLE but define a DISTINCT nested model (SubmissionLink) with tighter caps (label<=30, url<=220). Leave the admin FeaturedLink caps (label<=40) untouched.
- `FeaturedReleaseRequest` (line 195) — admin validator style to mirror (text_required, artistas_required, max_four_links); keeps its own admin caps — the public SubmissionRequest uses its own tighter caps.
- `JobRequest.must_be_youtube` (line 113) — YouTube URL normalizer returning https://www.youtube.com/watch?v=ID; REUSE its logic for youtube_url.
- `_write_json_private(path, payload)` (line 352) — atomic 0o600 JSON writer for fallback files.
- `_redis` — module-level redis client; `redis_lib.exceptions.ConnectionError/TimeoutError` guarded everywhere.
- Constants block (lines 39-43): FEATURED_KEY, FEATURED_HISTORY_KEY=52, UPDATES_HISTORY_KEY — mirror the naming for submissions.
- `_append_to_history` (line 430) — LPUSH/LTRIM + fallback pattern (analog for the write+fallback shape; but submissions use Hash+ZSet, NOT List).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add submission settings to config.py</name>
  <files>api/config.py</files>
  <read_first>
    - api/config.py (full file — copy the `field(default_factory=lambda: _safe_int(...))` pattern used
      by analyze_rate_limit_per_minute and max_queue_depth)
  </read_first>
  <behavior>
    - Settings() exposes submission_rate_limit_per_hour (default 3, env SUBMISSION_RATE_LIMIT_PER_HOUR)
    - Settings() exposes submissions_cap (default 200, env SUBMISSIONS_CAP)
    - Settings() exposes submissions_fallback_path (default ".data/submissions.json", env SUBMISSIONS_FALLBACK_PATH)
    - Settings() exposes featured_next_path (default ".data/featured-next.json", env FEATURED_NEXT_PATH)
  </behavior>
  <action>
    Add four frozen-dataclass fields mirroring the existing default_factory style: an int
    submission_rate_limit_per_hour via _safe_int("SUBMISSION_RATE_LIMIT_PER_HOUR", 3) (D-04), an int
    submissions_cap via _safe_int("SUBMISSIONS_CAP", 200) (D-02), and two string paths
    submissions_fallback_path and featured_next_path read from os.environ with the defaults above
    (mirroring featured_fallback_path / system_updates_path). Do not change existing fields.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "from api.config import Settings; s=Settings(); assert s.submission_rate_limit_per_hour==3 and s.submissions_cap==200 and s.submissions_fallback_path.endswith('submissions.json') and s.featured_next_path.endswith('featured-next.json')"` exits 0.
    - Env override works: `SUBMISSION_RATE_LIMIT_PER_HOUR=7 .venv/bin/python -c "from api.config import Settings; assert Settings().submission_rate_limit_per_hour==7"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "from api.config import Settings; s=Settings(); assert s.submission_rate_limit_per_hour==3; assert s.submissions_cap==200; assert s.submissions_fallback_path.endswith('submissions.json'); assert s.featured_next_path.endswith('featured-next.json'); print('OK')"</automated>
  </verify>
  <done>Four new settings read from env with safe defaults, matching the existing config style.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubmissionContact + SubmissionRequest Pydantic models</name>
  <files>api/main.py</files>
  <read_first>
    - api/main.py lines 108-234 (JobRequest.must_be_youtube, FeaturedArtist, FeaturedLink, FeaturedReleaseRequest — mirror the validator STYLE; do NOT reuse the admin caps for the public models)
    - .planning/phases/16-.../16-RESEARCH.md § "Pattern 4" and "Code Examples" (model_validator + tightened caps)
    - CLAUDE.md § Security Gate (Pydantic field_validator/model_validator required)
  </read_first>
  <behavior>
    - SubmissionArtist (nome<=100, url<=200) and SubmissionLink (label<=30, url<=220) are DISTINCT public nested models with TIGHTER per-field char caps than the admin FeaturedArtist/FeaturedLink (which keep their 200/500 and 40 caps for the admin featured flow — leave them unchanged)
    - SubmissionContact with instagram/telefone/email (all optional strings, trimmed, <=150 chars); model_validator(mode="after") raises when all three are empty (D-08)
    - SubmissionRequest requires >=1 artista (cap 3, SubmissionArtist), produtores optional (cap 1, SubmissionArtist), titulo/genero required (<=150 each), descricao required (<=350), youtube_url required + normalized like JobRequest, links optional (cap 4, SubmissionLink), contato: SubmissionContact required
    - SubmissionRequest has a decoy field named `website` (default "") — the honeypot; validation does not reject a filled value here (the endpoint handles it), it only exists on the model
    - Body budget (measured, not estimated): the MAXIMUM-CARDINALITY payload — every list slot full at its cap (3 artistas, 1 produtor, 4 links), every string field at its exact char cap (nome=100, artist url=200, link label=30, link url=220, titulo=150, genero=150, descricao=350), all 3 contact fields at 150, youtube_url normalized, website="" — serializes to 3726 bytes (`json.dumps(payload, ensure_ascii=False).encode('utf-8')`), which is under _MAX_BODY_BYTES=4096 with 370 bytes of margin. These caps are chosen so this TRUE worst case provably fits; do NOT raise _MAX_BODY_BYTES and do NOT add a /submissions path exception.
  </behavior>
  <action>
    Define SubmissionArtist(BaseModel) {nome (strip, non-empty, <=100), url (strip, http-validated, <=200,
    default "")} and SubmissionLink(BaseModel) {label (strip, non-empty, <=30), url (strip, http-validated,
    <=220)} as DISTINCT public nested models. Mirror the FeaturedArtist/FeaturedLink validator style but use
    these TIGHTER per-field char caps — do NOT reuse FeaturedArtist/FeaturedLink and do NOT change their admin
    caps. Define SubmissionContact(BaseModel) with instagram/telefone/email defaulting to "", a field_validator
    on all three that strips and caps at 150 chars, and a model_validator(mode="after") enforcing
    at-least-one-non-empty (D-08 — this is the codebase's first model_validator; import it from pydantic).
    Define SubmissionRequest(BaseModel) using SubmissionArtist for artistas (required, max 3) and produtores
    (max 1), and SubmissionLink for links (max 4); titulo/genero via a text_required validator (strip,
    non-empty, <=150 each); descricao via a text_required validator (strip, non-empty, <=350); youtube_url via
    a validator reusing JobRequest.must_be_youtube normalization (obrigatório — D-11); contato:
    SubmissionContact; and website: str = "" as the honeypot decoy. These tighter per-field AND list caps (NOT
    the admin caps) are what keep the TRUE maximum-cardinality public body (3726 bytes, measured) under
    _MAX_BODY_BYTES=4096 (Pitfall 2 / SEC-SUBMIT-04). Place the models next to FeaturedReleaseRequest. Do NOT
    loosen the global _MAX_BODY_BYTES and do NOT add a path exception; if a future field is added, re-measure the
    max-cardinality payload and tighten these public caps to preserve the margin.
  </action>
  <acceptance_criteria>
    - Valid payload constructs: `SubmissionRequest(**valid_dict)` succeeds and normalizes youtube_url to https://www.youtube.com/watch?v=<11char>.
    - All-empty contact raises ValidationError mentioning contato.
    - 4 artistas raises (cap 3); 2 produtores raises (cap 1); 5 links raises (cap 4).
    - Over-length per-field raises: nome>100, artist url>200, link label>30, link url>220, titulo/genero>150, descricao>350, contact field>150 each raise ValidationError.
    - Body-size guard (concrete, MEASURED at max cardinality): json.dumps of the maximum-cardinality payload — 3 artistas (nome=100 chars, url=200 chars each), 1 produtor (100/200), 4 links (label=30, url=220 each), titulo=150, genero=150, descricao=350, youtube_url normalized, contato with 3x150-char fields, website="" — measures exactly 3726 bytes (`json.dumps(payload, ensure_ascii=False).encode('utf-8')`), and `len(...) < 4096` is asserted in an inline check (measured margin 370 bytes). This is the ACTUAL maximum permitted by the caps, not a reduced-cardinality sample.
    - `.venv/bin/python -m pytest tests/test_security.py -k "requires_one_contact or validates_required" -q` shows these two move toward GREEN once the endpoint exists (still RED here — endpoint pending — but models import cleanly).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import json; from api.main import SubmissionRequest, SubmissionContact; d={'artistas':[{'nome':'A','url':''}],'produtores':[{'nome':'B','url':''}],'titulo':'t','genero':'g','youtube_url':'https://youtube.com/watch?v=abcdefghijk','descricao':'d','links':[],'contato':{'instagram':'@x'}}; m=SubmissionRequest(**d); assert m.youtube_url=='https://www.youtube.com/watch?v=abcdefghijk'
try:
    SubmissionContact(instagram='',telefone='',email=''); raise SystemExit('should have raised')
except Exception: pass
worst={'artistas':[{'nome':'x'*100,'url':'y'*200} for _ in range(3)],'produtores':[{'nome':'x'*100,'url':'y'*200} for _ in range(1)],'titulo':'t'*150,'genero':'g'*150,'youtube_url':'https://www.youtube.com/watch?v=abcdefghijk','descricao':'d'*350,'links':[{'label':'l'*30,'url':'u'*220} for _ in range(4)],'contato':{'instagram':'i'*150,'telefone':'t'*150,'email':'e'*150},'website':''}
n=len(json.dumps(worst, ensure_ascii=False).encode('utf-8'))
assert n < 4096, n
print('OK max-cardinality body bytes:', n, 'margin:', 4096-n)"</automated>
  </verify>
  <done>Models validate all required fields with distinct tighter-capped public nested models (produtores cap 1, link url<=220, descricao<=350), enforce at-least-one-contact, keep the TRUE maximum-cardinality body under 4KB (3726 bytes measured, 370 margin), and expose the website honeypot field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Submission storage (Hash + Sorted Set + fallback + cap eviction)</name>
  <files>api/main.py</files>
  <read_first>
    - api/main.py lines 39-43 (constants), 352-498 (_write_json_private, _append_to_history, _get_history, _load_featured, _save_featured), 553-563 (_featured_document)
    - .planning/phases/16-.../16-RESEARCH.md § "Pattern 1" and "Pitfall 4" (Hash+ZSet, status-filtered eviction)
  </read_first>
  <behavior>
    - _submission_document(request_body) returns a dict with id (uuid4), created_at_epoch, data_recebida (ISO date), status="pendente", and all submission fields including contato
    - _save_submission(doc) writes Hash submissions:data (field=id, value=JSON) + ZSet submissions:index (score=created_at_epoch), then enforces cap, then writes JSON fallback; Redis errors fall back gracefully
    - _load_submissions() returns all docs newest-first from Redis, or from the fallback file on Redis error
    - _get_submission(id) / _update_submission(doc) support in-place edit and status transitions
    - _enforce_submissions_cap() removes only status in {rejeitada, arquivada} when count > submissions_cap; Pendente/Promovida never evicted
  </behavior>
  <action>
    Add constants SUBMISSIONS_DATA_KEY="submissions:data" and SUBMISSIONS_INDEX_KEY="submissions:index"
    near the existing FEATURED_* constants. Implement _submission_document(request_body) mirroring
    _featured_document but adding created_at_epoch (time.time()), status "pendente", and contato
    (model_dump). Implement _save_submission / _get_submission / _update_submission / _load_submissions
    using _redis.hset/hget/hdel + zadd/zrange/zrem, each wrapped in the existing ConnectionError/
    TimeoutError guard, with a dict-keyed JSON fallback written via _write_json_private to
    settings.submissions_fallback_path. Implement _enforce_submissions_cap() that only evicts oldest
    entries whose status is rejeitada or arquivada (D-02 / Pitfall 4) — soft-overflow if the cap is
    exceeded solely by non-terminal entries. Add featured:next helpers _load_featured_next /
    _save_featured_next (Redis string FEATURED_NEXT_KEY="featured:next" + fallback to
    settings.featured_next_path) mirroring _load_featured/_save_featured — these are consumed by Plan 04.
  </action>
  <acceptance_criteria>
    - Round-trip: `_save_submission(_submission_document(model))` then `_load_submissions()` returns the doc with status "pendente".
    - Cap eviction (unit): with submissions_cap monkeypatched low, inserting N terminal (rejeitada) + 1 old pendente keeps the pendente and drops oldest terminal; test_submissions_cap_evicts_terminal_only turns GREEN.
    - featured:next helpers persist and reload a dict.
    - `.venv/bin/python -m pytest tests/test_security.py -k "cap_evicts_terminal" -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_security.py -k "cap_evicts_terminal" -q</automated>
  </verify>
  <done>Submissions persist in Hash+ZSet with dict-keyed JSON fallback; cap evicts only terminal states; featured:next helpers ready for Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (public submission payload) | Untrusted JSON crosses into Pydantic validation and Redis storage |
| stored submission → cap eviction | Automated deletion must not destroy active moderation items |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial of Service | Oversized submission body → memory exhaustion | mitigate | Tightened public per-field char caps (artist nome<=100/url<=200, link label<=30/url<=220, titulo/genero<=150, descricao<=350, contact<=150) AND list caps (artistas 3, produtores 1, links 4) make the TRUE maximum-cardinality body measure 3726 bytes (measured), under the existing 4KB `_limit_body_size` middleware with 370 bytes margin; no global limit increase and no path exception |
| T-16-09 | Tampering | Cap eviction deletes non-terminal (Pendente/Promovida) submissions | mitigate | `_enforce_submissions_cap` filters by status, evicting only rejeitada/arquivada; soft-overflow otherwise |
| T-16-04 | Information Disclosure | Contact data persisted alongside public fields | accept (this plan) | Contact stored in the doc but never exposed publicly — the public-response exclusion is enforced at the endpoint layer in Plan 03 (SEC-SUBMIT-05) |
| T-16-02 | Tampering | Malicious field content persisted (XSS payload in descricao/email) | accept (this plan) | Storage persists raw validated text; XSS-safe rendering is enforced where it is displayed (admin tab, Plan 06) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_security.py -k "cap_evicts_terminal" -q` exits 0.
- Models import cleanly; TRUE maximum-cardinality payload measures 3726 bytes < 4096 (370 margin).
- No change to _MAX_BODY_BYTES; no /submissions path exception.
</verification>

<success_criteria>
- Config exposes 4 new submission settings.
- SubmissionRequest/SubmissionContact validate per D-08/D-11 with model_validator and distinct tighter-capped nested models.
- Storage uses Hash+ZSet+fallback with status-aware cap eviction; featured:next helpers exist.
</success_criteria>

<output>
After completion, create `.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-02-SUMMARY.md`
</output>
</content>
</invoke>
