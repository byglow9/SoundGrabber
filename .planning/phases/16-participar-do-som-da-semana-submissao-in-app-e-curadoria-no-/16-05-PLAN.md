---
phase: 16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-
plan: 05
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - static/index.html
  - static/nav.js
  - static/about.html
autonomous: true
requirements:
  - SUBMIT-10
  - SUBMIT-11

must_haves:
  truths:
    - "The #section-participar section is a real Y2K table-based form (no flexbox/grid) that POSTs to /submissions"
    - "The form collects all D-11 fields plus the D-08 contact section and a decoy honeypot hidden off-canvas"
    - "Submitting shows an on-screen success message (fire-and-forget); the email/copy-template flow is gone"
    - "A short privacy/consent note is in the form and the Política de Privacidade in index.html and about.html reflects contact-data collection"
  artifacts:
    - path: "static/index.html"
      provides: "Real submission form + updated privacy section"
      contains: "action"
    - path: "static/nav.js"
      provides: "Form submit wiring to POST /submissions; template flow removed"
      contains: "/submissions"
    - path: "static/about.html"
      provides: "Privacy section reflecting contact-data collection"
      contains: "privacidade"
  key_links:
    - from: "static/nav.js form submit"
      to: "POST /submissions"
      via: "fetch with JSON body"
      pattern: "fetch\\(['\\\"]/submissions"
    - from: "honeypot input"
      to: "off-canvas CSS hiding"
      via: "position:absolute;left:-9999px (not display:none alone)"
      pattern: "left:-9999px"
---

<objective>
Replace the current email + copy-template "Participar" section with a real Y2K table-based
submission form that POSTs to /submissions, and update the privacy policy to reflect the new
collection of personal contact data (D-09).

Purpose: This is the user-visible half of the phase — the visitor now submits in-app instead of
copying a template and emailing. The form must honor the Y2K aesthetic (tables, no flexbox/grid, raw
hex, no CSS variables), include the honeypot decoy hidden via off-canvas CSS (not display:none), and
show a fire-and-forget success message (D-10). Privacy transparency is a locked in-scope consequence
of collecting Instagram/phone/email (D-09).
Output: form + privacy updates turning the SUBMIT-10/11 frontend stubs GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-CONTEXT.md
@.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-RESEARCH.md

<interfaces>
<!-- Existing markup/JS to modify. -->
From static/index.html:
- #section-participar (lines ~287-307) — current email/copy-template content to replace with the form.
- #section-privacidade (line ~257) — privacy section to update.
From static/nav.js:
- participarTemplate array + copy-template-btn/copy-template-status handlers (lines ~80-147) — remove.
- Existing section navigation (setSections / participarSections) — keep; the form lives in the same section.
From static/about.html:
- #privacidade section (line ~110) — mirror the index.html privacy update.
From the API (Plan 03): POST /submissions expects JSON with keys artistas[], produtores[], titulo,
genero, youtube_url, descricao, links[], contato{instagram,telefone,email}, website (honeypot).
Response is 202 {"status":"recebido"} on success (no contact echo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Y2K table-based submission form in index.html</name>
  <files>static/index.html</files>
  <read_first>
    - static/index.html lines 287-307 (#section-participar current markup) and the surrounding table structure to match the page's existing table idiom
    - .planning/phases/16-.../16-RESEARCH.md § "Pitfall 3" (honeypot naming/hiding) and Architectural Responsibility Map
    - CLAUDE.md § "Restrições críticas" (tables for layout, no flexbox/grid, raw hex, no CSS variables)
  </read_first>
  <action>
    Replace the body of #section-participar with a `<form id="participar-form">` laid out with
    `<table>` rows (label cell + input cell), collecting the D-11 fields: artista/grupo nome + link,
    produtor/beatmaker nome + link, titulo, genero, youtube_url (mark `required`), descricao
    (textarea), up to 4 extra links (label + url pairs). Add a "Dados de quem está enviando" fieldset
    with instagram, telefone, email inputs (none individually required — the at-least-one rule is
    server-enforced; add helper text "Preencha ao menos um contato"). Add a short privacy/consent note
    near the submit button (D-09) — e.g. that contact data is used only for the curators to reach out
    and is never shown publicly, linking to /static/about.html#privacidade. Add the honeypot decoy:
    an input named `website` inside a wrapper hidden via inline off-canvas CSS
    (position:absolute;left:-9999px) with tabindex="-1", aria-hidden="true", autocomplete="off" — do
    NOT name it "honeypot" and do NOT use display:none alone (Pitfall 3). Add an empty
    `<div id="participar-status" aria-live="polite">` for the success/error message. Use only
    table/fieldset markup and raw hex if any inline color is needed — no flexbox/grid, no CSS variables.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_frontend.py -k "participar_form_is_table_based or participar_form_required_youtube_field or participar_form_has_contact_section or participar_form_has_privacy_note or participar_honeypot_field_hidden_off_canvas" -q` exits 0.
    - `grep -q "left:-9999px" static/index.html` and `! grep -qi "id=\"honeypot\"" static/index.html`.
    - No forbidden CSS in the new markup: `grep -Ei "display:\s*flex|display:\s*grid|var\(--" static/index.html` returns nothing new for the participar section (inspect the diff).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_frontend.py -k "participar_form" -q && grep -q "left:-9999px" static/index.html</automated>
  </verify>
  <done>#section-participar is a Y2K table form with all fields, contact section, privacy note, and an off-canvas honeypot.</done>
</task>

<task type="auto">
  <name>Task 2: Wire form submit in nav.js; remove email/copy-template flow</name>
  <files>static/nav.js</files>
  <read_first>
    - static/nav.js full file (participarTemplate + copy-template handlers at ~80-147 to remove; keep section nav)
    - .planning/phases/16-.../16-CONTEXT.md D-10 (success message, fire-and-forget)
  </read_first>
  <action>
    Remove the participarTemplate array and the copy-template-btn/copy-template-status wiring. Add a
    submit handler for #participar-form that serializes the fields into the JSON shape POST /submissions
    expects (artistas/produtores/links as arrays of objects; contato object; website honeypot passed
    through as-is), fetch POSTs to /submissions, and on any 202 shows a fire-and-forget success message
    in #participar-status (D-10 — no reference code, no account), resetting the form. On 422/429/4xx,
    render a clear human-readable error in #participar-status using textContent (never innerHTML —
    XSS-safe). Do not add a captcha or any third-party script (D-03, CSP self).
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_frontend.py -k "participar_email_copy_template_removed" -q` exits 0.
    - `grep -q "/submissions" static/nav.js` and `! grep -q "participarTemplate" static/nav.js` and `! grep -q "copy-template-btn" static/nav.js`.
    - Status rendering uses textContent: `grep -q "participar-status" static/nav.js` and no `innerHTML` write to that node.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_frontend.py -k "participar_email_copy_template_removed" -q && grep -q "/submissions" static/nav.js && ! grep -q "participarTemplate" static/nav.js</automated>
  </verify>
  <done>Form POSTs to /submissions with a fire-and-forget success message; the mailto/copy-template flow is fully removed.</done>
</task>

<task type="auto">
  <name>Task 3: Update Política de Privacidade for contact-data collection</name>
  <files>static/index.html, static/about.html</files>
  <read_first>
    - static/index.html #section-privacidade (~line 257)
    - static/about.html #privacidade (~line 110)
    - .planning/phases/16-.../16-CONTEXT.md D-09 (privacy policy must reflect contact collection)
  </read_first>
  <action>
    Update the privacy sections in both files to disclose that the Participar form now collects
    optional personal contact data (Instagram, telefone, email — at least one), that this data is
    stored only for the curators to contact the submitter, is visible only to operators (never public),
    and is retained subject to the moderation-queue cap. Keep the existing Y2K prose/markup style and
    the existing anchors (#privacidade). Do not introduce tracking claims that contradict the
    no-analytics-of-PII statements already present — add the submission-contact disclosure as an
    additional paragraph.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_frontend.py -k "participar_form_has_privacy_note" -q` exits 0 (note references the policy) and the full frontend suite passes.
    - Both files mention contact-data collection: `grep -qi "contato" static/index.html && grep -qi "contato\|instagram\|telefone" static/about.html`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_frontend.py -q</automated>
  </verify>
  <done>Privacy policy in index.html and about.html discloses the new contact-data collection per D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor input → client-side serialization → POST /submissions | Client only serializes; all validation/enforcement is server-side |
| honeypot field → real users | Must be invisible to humans/screen-readers, visible to naive bots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Denial of Service | Bots auto-fill the form | mitigate | Off-canvas honeypot (`website`) hidden with position:absolute;left:-9999px + tabindex=-1/aria-hidden (not display:none); server drops filled honeypot (Plan 03) |
| T-16-02 | Tampering | XSS via error/success message rendering | mitigate | #participar-status updated via textContent only, never innerHTML |
| T-16-12 | Information Disclosure | Missing privacy disclosure for collected PII | mitigate | D-09 privacy note in the form + Política de Privacidade updated in both index.html and about.html |
| T-16-13 | Spoofing | Third-party captcha/tracker script introduced | accept/avoid | No captcha or external script added (D-03); CSP `self` preserved |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_frontend.py -q` all GREEN.
- No flexbox/grid/CSS-variables introduced; honeypot off-canvas (not display:none).
- Email/copy-template flow removed; form POSTs to /submissions.
</verification>

<success_criteria>
- Real Y2K submission form with contact section, privacy note, and off-canvas honeypot.
- Fire-and-forget success message; privacy policy updated in both files.
</success_criteria>

<output>
After completion, create `.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-05-SUMMARY.md`
</output>
