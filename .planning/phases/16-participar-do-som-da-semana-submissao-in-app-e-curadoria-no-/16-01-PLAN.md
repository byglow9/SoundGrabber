---
phase: 16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/test_security.py
  - tests/test_frontend.py
autonomous: true
requirements:
  - SUBMIT-01
  - SUBMIT-02
  - SUBMIT-03
  - SUBMIT-04
  - SUBMIT-05
  - SUBMIT-06
  - SUBMIT-07
  - SUBMIT-08
  - SUBMIT-09
  - SUBMIT-10
  - SUBMIT-11
  - SEC-SUBMIT-01
  - SEC-SUBMIT-02
  - SEC-SUBMIT-03
  - SEC-SUBMIT-04
  - SEC-SUBMIT-05

must_haves:
  truths:
    - "REQUIREMENTS.md has a v1.4 section defining SUBMIT-01..11 and SEC-SUBMIT-01..05 with a traceability row each"
    - "tests/test_security.py contains RED stubs for the public submission endpoint and every admin curation endpoint"
    - "tests/test_frontend.py contains RED stubs for the public form markup contract and the Yonkou Submissões tab"
    - "Running the security + frontend suites shows the new tests failing (RED) with no collection errors"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "New v1.4 REQ-IDs for submission + curation feature and security controls"
      contains: "SEC-SUBMIT-01"
    - path: "tests/test_security.py"
      provides: "RED security stubs for submissions + curation"
      contains: "test_post_submission_returns_success"
    - path: "tests/test_frontend.py"
      provides: "RED frontend contract stubs for form + admin tab"
      contains: "section-participar"
  key_links:
    - from: "tests/test_security.py"
      to: "api_client fixture"
      via: "existing conftest fakeredis + MemoryStorage fixture"
      pattern: "def test_.*\\(api_client"
---

<objective>
Establish the Wave 0 (TDD RED) baseline for Phase 16: define the new v1.4 requirement IDs
(SUBMIT-* functional + SEC-SUBMIT-* security) and author the failing test stubs in
tests/test_security.py and tests/test_frontend.py that every downstream implementation plan
must turn GREEN.

Purpose: Nyquist compliance — every implementation task in later plans maps to a concrete
automated test that exists BEFORE the code is written. Security Gate requires
tests/test_security.py coverage for the new public write endpoint and admin curation endpoints.
Output: REQUIREMENTS.md v1.4 section + failing (RED) test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-CONTEXT.md
@.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-RESEARCH.md

<interfaces>
<!-- Existing test conventions the stubs must mirror. Executor uses these directly. -->
From tests/conftest.py:
- Fixture `api_client` provides a FastAPI TestClient with fakeredis (in-memory) and a
  limits.storage.MemoryStorage rate-limit backend — no network, no Docker.
From tests/test_security.py (existing analog tests to copy style from):
- test_post_featured_requires_operator_session, test_post_featured_validates_links,
  test_post_featured_rate_limit, test_yonkou_mutation_requires_csrf_token,
  test_featured_redis_fallback — these show how to obtain an admin session cookie + CSRF
  token, how to assert 401/403, and how to assert rate-limit 429.
- test_yonkou_updates_form_is_hidden_behind_button (line ~802) — shows how to fetch the
  authenticated /yonkou panel HTML and assert markup is present/hidden (analog for the
  Submissões tab markup stub).
From tests/test_frontend.py (existing analog test):
- test_html_required_ids_present (line 84) — shows how to fetch static HTML and assert a
  markup contract on the public page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define v1.4 REQ-IDs in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the full file — copy the exact section + Traceability table format used for v1.1 SEC-* and v1.3 blocks)
    - .planning/phases/16-.../16-CONTEXT.md (D-01..D-12 — each REQ-ID traces to a decision)
  </read_first>
  <action>
    Add a new "## v1.4 Requirements — Participar do Som da Semana (submissão in-app + curadoria)"
    section after the v1.3 block. Define functional REQ-IDs under a `SUBMIT` category and security
    REQ-IDs under a `SEC-SUBMIT` category, each as an unchecked checkbox with a one-line description
    tracing to its decision:
    SUBMIT-01 (POST /submissions accepts a valid submission and returns a generic success, D-10),
    SUBMIT-02 (Pydantic validates required fields: artista, titulo, genero, youtube_url obrigatório, descricao — D-11),
    SUBMIT-03 (at-least-one-contact rule via model_validator — D-08),
    SUBMIT-04 (admin GET /yonkou/submissions lists all submissions with status — D-03/D-05),
    SUBMIT-05 (admin PATCH edits a submission in-place — D-07),
    SUBMIT-06 (admin promote derives featured:next without publishing — D-06/D-07),
    SUBMIT-07 (admin publish moves featured:next → featured:current, old current → featured:history — D-06),
    SUBMIT-08 (admin reject/archive status transitions — D-05),
    SUBMIT-09 (retention cap ~200 evicts only terminal rejeitada/arquivada entries — D-02),
    SUBMIT-10 (public #section-participar replaced by a real Y2K table form that POSTs to /submissions, email/copy-template flow removed — D-11),
    SUBMIT-11 (privacy consent note in the form + Política de Privacidade updated in index.html #section-privacidade and about.html — D-09),
    SEC-SUBMIT-01 (POST /submissions rate limited ~3/hour per IP, configurable via env — D-04),
    SEC-SUBMIT-02 (filled honeypot → silent fake-success, no persistence — D-03),
    SEC-SUBMIT-03 (all admin submission mutations under _admin_csrf_dependency — D-12),
    SEC-SUBMIT-04 (POST /submissions body-size enforced via tightened Pydantic caps or dedicated exception — D-12),
    SEC-SUBMIT-05 (contact fields never appear in any public/unauthenticated response — D-08).
    Append one row per REQ-ID to the Traceability table with `| Phase 16 | Pending |`. Use the
    proposed key names `submissions:data` / `submissions:index` / `featured:next` in descriptions
    only where helpful; do not lock storage internals here.
  </action>
  <acceptance_criteria>
    - `grep -c "SUBMIT-" .planning/REQUIREMENTS.md` returns at least 16 (11 SUBMIT + 5 SEC-SUBMIT definitions, plus traceability rows means the count is higher — assert >= 16).
    - Every one of SUBMIT-01..11 and SEC-SUBMIT-01..05 appears at least once: `for id in SUBMIT-01 ... SEC-SUBMIT-05; do grep -q "$id" .planning/REQUIREMENTS.md; done` all succeed.
    - A "## v1.4 Requirements" heading exists.
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq "v1.4 Requirements" .planning/REQUIREMENTS.md && grep -q "SEC-SUBMIT-05" .planning/REQUIREMENTS.md && grep -q "SUBMIT-11" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>REQUIREMENTS.md v1.4 section defines all 16 new REQ-IDs with traceability rows.</done>
</task>

<task type="auto">
  <name>Task 2: RED security stubs for submissions + curation</name>
  <files>tests/test_security.py</files>
  <read_first>
    - tests/test_security.py (read the full file — reuse the admin-session/CSRF helper pattern from
      test_post_featured_* and test_yonkou_mutation_requires_csrf_token; reuse the 429 rate-limit
      assertion style from test_post_featured_rate_limit; reuse the fallback pattern from
      test_featured_redis_fallback)
    - tests/conftest.py (api_client fixture — fakeredis + MemoryStorage)
    - .planning/phases/16-.../16-RESEARCH.md § "Phase Requirements → Test Map" (exact test names to use)
  </read_first>
  <action>
    Add failing stubs (each asserting the intended final behavior, so they are RED until the
    endpoints exist) using the exact names from the research test map:
    test_post_submission_returns_success (POST /submissions with a valid payload → 202 and a body
    that does NOT contain contact fields),
    test_post_submission_validates_required_fields (missing titulo/genero/youtube_url/descricao → 422
    unified {error,error_type}),
    test_post_submission_requires_one_contact (all contact fields empty → 422),
    test_submission_rate_limit (>3 posts within the window → 429 with Retry-After),
    test_submission_honeypot_silently_dropped (honeypot/website field filled → 202 identical success
    AND nothing persisted in submissions storage),
    test_submission_body_size_enforced (oversized payload → 413 or 422, never 500),
    test_submission_response_excludes_contact (SEC-SUBMIT-05 — response JSON has no instagram/telefone/email),
    test_get_submissions_requires_admin (GET /yonkou/submissions without session → 401),
    test_patch_submission_requires_csrf (PATCH /yonkou/submissions/{id} without CSRF → 403),
    test_promote_submission_writes_featured_next (promote → featured:next populated, featured:current unchanged),
    test_publish_next_moves_current_to_history (publish → old featured:current appended to featured:history, featured:next becomes current),
    test_reject_and_archive_submission (reject then archive → status transitions persisted),
    test_submission_admin_mutations_require_csrf (promote/publish/reject/archive all 403 without CSRF token),
    test_submissions_cap_evicts_terminal_only (D-02 — oldest Pendente survives, oldest rejeitada evicted when over cap).
    Where an endpoint does not yet exist, the request will 404/405 and the assertion fails — that is
    the intended RED state. Do NOT implement any production code.
  </action>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_security.py` increases by exactly 14 versus the pre-edit count.
    - All 14 named functions exist: `for t in test_post_submission_returns_success test_post_submission_validates_required_fields test_post_submission_requires_one_contact test_submission_rate_limit test_submission_honeypot_silently_dropped test_submission_body_size_enforced test_submission_response_excludes_contact test_get_submissions_requires_admin test_patch_submission_requires_csrf test_promote_submission_writes_featured_next test_publish_next_moves_current_to_history test_reject_and_archive_submission test_submission_admin_mutations_require_csrf test_submissions_cap_evicts_terminal_only; do grep -q "def $t" tests/test_security.py; done` succeeds.
    - The file collects without ImportError: `.venv/bin/python -m pytest tests/test_security.py --collect-only -q` exits 0.
    - The new tests are RED: `.venv/bin/python -m pytest tests/test_security.py -k submission -q` exits non-zero (failures, not collection errors).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_security.py --collect-only -q >/dev/null && ! .venv/bin/python -m pytest tests/test_security.py -k "submission or promote or publish_next" -q; test $? -eq 0 && echo "RED-OK"</automated>
  </verify>
  <done>14 named submission/curation security tests exist and fail (RED); no collection errors.</done>
</task>

<task type="auto">
  <name>Task 3: RED frontend contract stubs for form + admin tab</name>
  <files>tests/test_frontend.py</files>
  <read_first>
    - tests/test_frontend.py (read the full file — reuse the static-HTML fetch + markup assertion
      style from test_html_required_ids_present at line 84)
    - tests/test_security.py (test_yonkou_updates_form_is_hidden_behind_button, line ~802 — reuse its
      authenticated /yonkou panel HTML fetch + markup assertion style for the Submissões tab stub)
    - static/index.html #section-participar (lines ~287-307 — current email/copy-template markup being replaced)
    - .planning/phases/16-.../16-RESEARCH.md § "Wave 0 Gaps" (frontend contract list)
  </read_first>
  <action>
    Add failing stubs asserting the final form + admin markup contract:
    test_participar_form_is_table_based (#section-participar contains a <form> and <table> markup,
    and no flexbox/grid — VISUAL-04),
    test_participar_form_required_youtube_field (form has a required youtube_url input — D-11),
    test_participar_form_has_contact_section (form has instagram, telefone, email inputs — D-08),
    test_participar_form_has_privacy_note (form contains a short consent/privacy note — D-09),
    test_participar_honeypot_field_hidden_off_canvas (a decoy field exists whose CSS hiding uses
    off-canvas positioning like position:absolute;left:-9999px, NOT display:none alone, and is not
    literally named "honeypot" in the markup — Pitfall 3),
    test_participar_email_copy_template_removed (copy-template-btn / participarTemplate mailto flow
    is gone),
    test_yonkou_has_submissions_tab (operator panel HTML from GET /yonkou exposes a Submissões tab
    button id, e.g. tab-submissoes-btn).
    These are RED until the form/tab exist. No production code.
  </action>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_frontend.py` increases by exactly 7.
    - `.venv/bin/python -m pytest tests/test_frontend.py --collect-only -q` exits 0.
    - New tests are RED: `.venv/bin/python -m pytest tests/test_frontend.py -k "participar or submissoes" -q` exits non-zero (failures).
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_frontend.py --collect-only -q >/dev/null && ! .venv/bin/python -m pytest tests/test_frontend.py -k "participar or submissoes" -q; test $? -eq 0 && echo "RED-OK"</automated>
  </verify>
  <done>7 frontend contract stubs exist and fail (RED); no collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test authorship → later implementation | Stubs define the security contract; weak stubs let insecure code pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Repudiation | Missing test coverage for a security control | mitigate | Every SEC-SUBMIT-* control has a named RED stub in this plan; downstream plans cannot be marked done until GREEN |
| T-16-08 | Elevation of Privilege | Honeypot detection observable to bots | mitigate | test_submission_honeypot_silently_dropped asserts identical 202 success + no persistence, forbidding a distinguishable error path |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_security.py tests/test_frontend.py --collect-only -q` exits 0 (no collection errors).
- New submission/curation/form tests are RED (expected — no implementation yet).
- All 16 REQ-IDs present in REQUIREMENTS.md.
</verification>

<success_criteria>
- REQUIREMENTS.md defines SUBMIT-01..11 and SEC-SUBMIT-01..05 with traceability rows.
- 14 new security stubs + 7 new frontend stubs exist and fail cleanly (RED).
</success_criteria>

<output>
After completion, create `.planning/phases/16-participar-do-som-da-semana-submissao-in-app-e-curadoria-no-/16-01-SUMMARY.md`
</output>
</output>
